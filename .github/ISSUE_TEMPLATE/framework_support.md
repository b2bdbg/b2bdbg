---
name: Framework support problem
about: A bot framework / client library does not work correctly behind the proxy
labels: framework-support
---

## Library and version

- Library (e.g. python-telegram-bot, aiogram, grammY, Telegraf, go-telegram-bot-api):
- Library version:
- Language / runtime version:

## Base-URL configuration used

<!-- Paste the exact base-URL / apiRoot / api template you set. Redact the
     real token. The expected path b2bdbg parses is /bot<TOKEN>/<method>. -->

```
```

## What happened

<!-- What did the bot / library do? Errors, no traffic through b2bdbg, wrong
     path, etc. What did you expect instead? -->

## /healthz output

<!-- Output of:  curl -fsS http://localhost:8080/healthz  (should print "ok") -->

```
```

## Relevant span / log

<!-- Paste b2bdbg log lines (JSON) and/or span output for the failing call.
     Redact any real bot tokens before posting. -->

```
```

## Additional context

<!-- Long-poll or webhook? Custom config? See docs/frameworks.md for the
     canonical per-library base-URL form. -->
