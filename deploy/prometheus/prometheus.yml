global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: b2bdbg
    # b2bdbg exposes /metrics on the same port as the proxy (default :8080).
    static_configs:
      - targets: ["b2bdbg:8080"]
    metrics_path: /metrics
    scrape_interval: 10s
